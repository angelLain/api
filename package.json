{
  "name": "my_api",
  "version": "1.0.0",
  "description": "nada",
  "main": "dist/app.js",
  "scripts": {
    "start": "tsc && node dist/app.js",
    "postinstall": "npm run compile:deps",
    "compile:deps": "tsc -p ./ --skipLibCheck",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.13.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "html-pdf-node": "^1.0.8"
  }
}
